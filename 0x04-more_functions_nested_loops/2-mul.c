#include "main.h"

/**
 *mul-  prints times_tablede multiplication from two values
 *@a: the first value
 *@b: the second value
 *Return: always 0
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	_putchar(result + '0');
	return (0);
}
