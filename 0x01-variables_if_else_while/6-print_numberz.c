#include <stdio.h>

/**
 * main - print the 0-9
 * Description: prints the 0,9 using putchar
 * Return: 0.
 */
int main(void)
{
	int ch;

	for (ch = 1; ch <= 9; ch++)
	putchar (ch);
	putchar ('\n');
	return (0);
}
